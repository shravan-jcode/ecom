# Install required packages (run these commands in terminal before running code)
# pip install pandas
# pip install scikit-learn

import pandas as pd
from sklearn.preprocessing import StandardScaler, MinMaxScaler

# Load dataset
data = pd.read_csv("cars.csv")

# Remove commas from Mileage and convert to integer
data["Mileage"] = data["Mileage"].str.replace(",", "").astype(int)

# Select numerical columns
X = data[["Mileage","Sell Price"]]

# -------- Standardization --------
scaler = StandardScaler()
standardized = scaler.fit_transform(X)

print("Standardized Data:")
print(standardized)

# -------- Normalization --------
normalizer = MinMaxScaler()
normalized = normalizer.fit_transform(X)

print("\nNormalized Data:")
print(normalized)